{{- if .Values.enabled }}
{{- if .Values.enabledDeployment }}
apiVersion: apps/v1
kind: Deployment
{{- $name := .Values.name | default .Release.Name }}
metadata:
  name: "{{ $name }}"
  labels:
    app.kubernetes.io/name: "{{ $name }}"
    {{- range $key, $value := .Values.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: "{{ $name }}"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "{{ $name }}"
        {{- range $key, $value := .Values.labels }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- range . }}
      - name: "{{ . }}"
      {{- end }}
      {{- end }}
      terminationGracePeriodSeconds: 5 # TODO: better signal handling in docker-ros images would be preferred?
      {{- if .Values.gpu }}
      runtimeClassName: nvidia
      {{- end }}
      {{- with .Values.initContainers }}
      initContainers:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: "{{ $name }}"
        image: "{{ .Values.image }}"
        imagePullPolicy: "{{ .Values.imagePullPolicy }}"
        {{- with .Values.entrypoint }}
        command:
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.command }}
        args:
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.shm }}
        securityContext:
          capabilities:
            add:
            - IPC_LOCK
        {{- end }}
        env:
        {{- if .Values.gpu }}
        - name: NVIDIA_VISIBLE_DEVICES
          value: "all"
        - name: NVIDIA_DRIVER_CAPABILITIES
          value: "all"
        {{- end }}
        {{- if .Values.x11Display }}
        - name: DISPLAY
          value: "{{ .Values.x11Display }}"
        - name: XAUTHORITY
          value: /.Xauthority
        {{- end }}
        {{- range $key, $value := .Values.environment }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        volumeMounts:
        - name: timezone
          mountPath: /etc/timezone
        - name: localtime
          mountPath: /etc/localtime
        {{- if .Values.shm }}
        - name: shared-memory
          mountPath: /dev/shm
        {{- end }}
        {{- if .Values.x11XauthorityFile }}
        - name: x11-unix
          mountPath: /tmp/.X11-unix
        - name: xauthority
          mountPath: /.Xauthority
        {{- end }}
        {{- if and .Values.rosParamFileMountPath .Values.rosParamFileData }}
        - name: "{{ include "rfc1123CompliantName" (.Values.rosParamFileMountPath | base) }}"
          mountPath: "{{ .Values.rosParamFileMountPath }}"
          subPath: "{{ .Values.rosParamFileMountPath | base }}"
        {{- end }}
        {{- range .Values.volumes }}
        {{- if or (and .data (not (empty (tpl .data $)))) .hostPath }}
        - name: "{{ include "rfc1123CompliantName" (.mountPath | base) }}"
          mountPath: "{{ .mountPath }}"
        {{- if .data }}
          subPath: "{{ .mountPath | base }}"
        {{- end }}
        {{- end }}
        {{- end }}
      volumes:
      - name: timezone
        hostPath:
          path: /etc/timezone
      - name: localtime
        hostPath:
          path: /etc/localtime
      {{- if .Values.shm }}
      - name: shared-memory
        hostPath:
          path: /dev/shm
          type: Directory
      {{- end }}
      {{- if .Values.x11XauthorityFile }}
      - name: x11-unix
        hostPath:
          path: /tmp/.X11-unix
      - name: xauthority
        hostPath:
          path: "{{ .Values.x11XauthorityFile }}"
      {{- end }}
      {{- if and .Values.rosParamFileMountPath .Values.rosParamFileData }}
      - name: "{{ include "rfc1123CompliantName" (.Values.rosParamFileMountPath | base) }}"
        configMap:
          name: "{{ include "rfc1123CompliantName" (printf "%s-%s" (.Values.name | default .Release.Name) (.Values.rosParamFileMountPath | base)) }}"
      {{- end }}
      {{- range .Values.volumes }}
      {{- if or (and .data (not (empty (tpl .data $)))) .hostPath }}
      - name: "{{ include "rfc1123CompliantName" (.mountPath | base) }}"
      {{- if .data }}
        configMap:
          name: "{{ include "rfc1123CompliantName" (printf "%s-%s" ($.Values.name | default $.Release.Name) (.mountPath | base)) }}"
      {{- else if .hostPath }}
        hostPath:
          path: "{{ tpl .hostPath $ }}"
      {{- end }}
      {{- end }}
      {{- end }}
{{- end }}
{{- end }}
