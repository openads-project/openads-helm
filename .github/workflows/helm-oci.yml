name: helm-oci

on:
  push:
    branches:
      - main
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  helm-oci-openadservice:
    name: Publish openadservice
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Temporarily modify chart version to '1.2.3-<GIT-REF>'
        if: ${{ github.ref_name != 'main' }}
        shell: bash
        env:
          CHART_FOLDER: charts/openadservice
          REF_NAME: ${{ github.head_ref || github.ref_name }}
        run: |
          chart_file="${CHART_FOLDER}/Chart.yaml"
          base_version=$(sed -nE "s/^version:[[:space:]]*([^[:space:]#]+).*$/\1/p" "${chart_file}")
          ref_slug=$(echo "${REF_NAME}" | sed -E "s/[^a-zA-Z0-9-]+/-/g; s/^-+//; s/-+$//")
          if [[ -z "${base_version}" || -z "${ref_slug}" ]]; then
            echo "Could not determine a valid chart version or Git ref slug" >&2
            exit 1
          fi
          chart_version="${base_version}-${ref_slug}"
          sed -i -E "s/^version:.*$/version: ${chart_version}/" "${chart_file}"
          echo "Publishing chart version ${chart_version}"
      - name: Push Helm chart as OCI artifact
        uses: bsord/helm-push@5ec3320fb5720a0a5aa210d871999f2b836f2d97
        with:
          useOCIRegistry: true
          registry-url:  oci://ghcr.io/${{ github.repository }}
          username: ${{ github.actor }}
          access-token: ${{ secrets.GITHUB_TOKEN }}
          chart-folder: charts/openadservice
          update-dependencies: true
          force: false

  helm-oci:
    name: Publish ${{ matrix.chart }}
    needs: helm-oci-openadservice
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chart:
          - triton-server
          - zenoh-router
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Temporarily modify chart version to '1.2.3-<GIT-REF>'
        if: ${{ github.ref_name != 'main' }}
        shell: bash
        env:
          CHART_FOLDER: charts/${{ matrix.chart }}
          REF_NAME: ${{ github.head_ref || github.ref_name }}
        run: |
          chart_file="${CHART_FOLDER}/Chart.yaml"
          base_version=$(sed -nE "s/^version:[[:space:]]*([^[:space:]#]+).*$/\1/p" "${chart_file}")
          ref_slug=$(echo "${REF_NAME}" | sed -E "s/[^a-zA-Z0-9-]+/-/g; s/^-+//; s/-+$//")
          if [[ -z "${base_version}" || -z "${ref_slug}" ]]; then
            echo "Could not determine a valid chart version or Git ref slug" >&2
            exit 1
          fi
          chart_version="${base_version}-${ref_slug}"
          sed -i -E "s/^version:.*$/version: ${chart_version}/" "${chart_file}"
          echo "Publishing chart version ${chart_version}"
      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.18.4
      - name: Push Helm chart as OCI artifact
        shell: bash
        env:
          CHART_FOLDER: charts/${{ matrix.chart }}
          HELM_OCI_REGISTRY: ghcr.io/${{ github.repository }}
          HELM_REGISTRY_USERNAME: ${{ github.actor }}
          HELM_REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          printf '%s' "${HELM_REGISTRY_PASSWORD}" | helm registry login ghcr.io --username "${HELM_REGISTRY_USERNAME}" --password-stdin
          HELM_OCI_REGISTRY="${HELM_OCI_REGISTRY,,}"
          helm dependency update "${CHART_FOLDER}"
          mkdir -p /tmp/helm-packages
          helm package "${CHART_FOLDER}" --destination /tmp/helm-packages
          for chart_package in /tmp/helm-packages/*.tgz; do
            helm push "${chart_package}" "oci://${HELM_OCI_REGISTRY}"
          done
